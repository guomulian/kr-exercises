/*

If getchar() has the same value as EOF, getchar() != EOF will be false, or 0
If getchar() has a different value from EOF, getchar() != EOF will be true, or 1

This is "undesired" in the copy program since c is meant to be the input character
*/